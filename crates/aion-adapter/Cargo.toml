[package]
name = "aion-adapter"
description = "AION 系统适配层 — Linux Adapter: process / fs / net / cgroup / namespace / seccomp / device / capability"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tokio.workspace = true
thiserror.workspace = true
async-trait.workspace = true

[target.'cfg(unix)'.dependencies]
# kill() 等 #[cfg(unix)] 代码路径需要 libc（不限于 Linux，macOS 同样是 unix）
libc = "0.2"
